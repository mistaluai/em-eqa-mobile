import { StyleSheet, ViewStyle } from 'react-native';
import { SPACING } from '../../spacing';

export const ClipUploadSyncScreenStyles = StyleSheet.create({
  bottomSpacer: {
    height: SPACING.s32,
  } as ViewStyle,
});

