import { StyleSheet, ViewStyle } from 'react-native';

export const LoaderComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  } as ViewStyle,
});

